#include <stdio.h>

int main ()
{
    float numero ordini;
    float prezzo=5;
    float totale;
    printf("Inserisci numero ordini/n");
    scanf("%f", &numero ordini);
    x=numero ordini

    if ("anno%4 == 0 && anno%100 != 0 || anno%400 == 0")
       {printf("anno bisestile"); } 
    else
        {printf("anno non bisestile"); }

    return 0;
}